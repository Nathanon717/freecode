{
  "name": "slash-model-list",
  "description": "/models aliases /model with no arg and prints current model without crashing",
  "requiresLlm": false,
  "turns": [
    { "input": "/models" }
  ],
  "expect": {
    "stdoutContains": ["Current model:"],
    "stdoutAbsent": ["Error:"],
    "exitCode": 0
  }
}
