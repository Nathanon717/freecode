{
  "name": "slash-stray-confirmation",
  "description": "Sending 'y' with no pending tool call is silently skipped rather than forwarded to the agent",
  "requiresLlm": false,
  "turns": [
    { "input": "y" }
  ],
  "expect": {
    "stdoutContains": ["No pending tool request; skipping scripted confirmation", "Goodbye"],
    "stdoutAbsent": ["Error:"],
    "exitCode": 0
  }
}
