{
  "name": "blocklist-purge-script-mode",
  "description": "A scripted (non-TTY) run never offers the blocklist purge and never deletes the seeded blocklisted model — the confirmation is unanswerable without a terminal, so the rows must be left alone",
  "seedModels": [
    { "provider": "groq", "modelId": "whisper-large-v3", "displayName": "Whisper Large v3" }
  ],
  "turns": [
    { "input": "/status" }
  ],
  "expect": {
    "stdoutContains": ["Database"],
    "stdoutAbsent": [
      "blocklisted in the provider registry",
      "Enter to delete",
      "Deleted 1 model",
      "Error:"
    ],
    "exitCode": 0
  }
}
