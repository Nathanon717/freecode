{
  "name": "04-eval-shell-exec-write",
  "description": "Easy: run a shell command, write its output to a file",
  "requiresLlm": true,
  "config": {
    "toolRationale": false,
    "useOllama": false
  },
  "workspace": "temp",
  "turns": [
    {
      "input": "Use shell_exec to run the command `node -e \"process.stdout.write('SHELL_OK\\n')\"`. Then use write_file to create result.txt with content exactly \"SHELL_OK\\n\". Do not use any other tools."
    },
    { "input": "y" },
    { "input": "y" }
  ],
  "expect": {
    "stdoutAbsent": ["Error:"],
    "exitCode": 0,
    "files": [
      {
        "path": "result.txt",
        "contentExact": "SHELL_OK\n"
      }
    ],
    "toolTrace": {
      "maxCalls": 2,
      "sequence": ["shell_exec", "write_file"],
      "absent": ["read_file", "grep", "list_dir"]
    }
  }
}
