{
  "name": "agent-preamble-flush",
  "description": "A pre-tool-call preamble with no trailing newline is flushed in its correct position (before the tool call) instead of being held in the markdown line buffer and glued onto the final step's text. Asserted as an exact stdout block, so the documented spacing is pinned too: one blank line between the preamble and the call, the rationale directly above the call line with no blank, the created-content preview directly under it, and one blank line either side of the step divider. The approval chrome between them is spanned by a gap - its row count is not this test's contract",
  "workspace": "temp",
  "model": "mock-native:gpt-freecode-test",
  "llmFixture": "agent-preamble-flush.llm.json",
  "turns": [
    { "input": "Write grade.py then grade the run." },
    { "input": "y" }
  ],
  "expect": {
    "stdoutAbsent": ["change.The script"],
    "stdoutBlock": [
      "Let's make this change.",
      "",
      "Writing the grading script as requested",
      "create(grade.py)",
      "  1: print('grade: A')",
      "...",
      "",
      "re:^─+$",
      "",
      "The script now runs successfully and prints the grade summary."
    ],
    "exitCode": 0,
    "files": [
      { "path": "grade.py", "contentExact": "print('grade: A')\n" }
    ],
    "fakeLlmTrace": {
      "callCount": 2,
      "maxCalls": 2,
      "calls": [
        {
          "provider": "mock-native",
          "executionPath": "native-stream",
          "emittedTextContains": ["Let's make this change."],
          "emittedToolCalls": ["create"]
        },
        {
          "provider": "mock-native",
          "executionPath": "native-stream",
          "emittedTextContains": ["The script now runs successfully"]
        }
      ]
    }
  }
}
