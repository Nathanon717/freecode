node_modules/
dist/
coverage/

# secrets
.env
.env.*
!.env.example

# planning / private notes
dont look/
docs/.obsidian/

# humaneval dataset (large binary — see evals/humaneval/README.md)
evals/humaneval/data/
__pycache__/
*.pyc

# generated
*.js.map

# map-drift model-comparison reports (one per model per run)
scripts/diagnostics/map-drift/
*.d.ts.map
poe-cookies.json

# diagnostic script run-output (regenerated on demand)
scripts/diagnostics/model-availability-results.txt
scripts/diagnostics/local-tokenizer-results.txt

# local DB (synced via Turso — not committed to git)
.freecode/freecode.db
.freecode/freecode.db-shm
.freecode/freecode.db-wal
.freecode/freecode.db-info

# V8 profiler artifacts
isolate-*.log

# eslint incremental cache
.eslintcache

# local store files — data now lives in freecode.db (synced via Turso)
.freecode/models.json
.freecode/evals/
.freecode/model-cache.json
# boot fast-path mirror of the DB config cache — local, rebuilt from the DB
.freecode/config-cache.json

# downloaded tokenizer files (tokenizer.json, .model, rank files) — fetched on demand
.freecode/tokenizers/

# eval run artifacts — data now lives in freecode.db
evals/custom/results/
evals/custom/**/.artifacts/
evals/custom/**/.run/
evals/custom/**/work/
evals/humaneval/.runs/

# opencode source checkout
opencode source code/

# repomix output
repomix/
