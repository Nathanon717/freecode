# Bug Log

- [26-06-2026.md](26-06-2026.md) — WalConflict crash on every menu open; raw control bytes in scenario JSON; pre-tool preamble printed in wrong place
- [27-06-2026.md](27-06-2026.md) — `/models` stall from synced/plain replica ping-pong corrupting `-info` metadata
- [27-06-2026b.md](27-06-2026b.md) — Eval menu artifacts visible above first eval output
- [02-07-2026.md](02-07-2026.md) — PTY test harness flaky on Windows: bare `/` auto-submitted; first keystroke dropped before ConPTY ready
- [02-07-2026b.md](02-07-2026b.md) — Re-bootstrap fix from 27-06 never landed; implemented for real with coverage
- [03-07-2026.md](03-07-2026.md) — No shared text-encoding layer; BOM and duplicated backspace-check bugs kept recurring
- [05-07-2026.md](05-07-2026.md) — Tokenizer families silently using fallback estimate due to 0-byte cache files from missed 307 redirects
- [11-07-2026.md](11-07-2026.md) — Pre-tool preamble rendered after the tool call; fixed by driving the transcript from the ordered `fullStream` + a tool-render rendezvous gate
- [18-07-2026b.md](18-07-2026b.md) — Every Mistral model 400s mid-session: an empty assistant turn stored in history has neither content nor `tool_calls`
- [18-07-2026c.md](18-07-2026c.md) — Empty store when launched outside the repo: unpinned `doppler run` resolves its project from cwd, and the wrapper existed only as a PowerShell function
- [14-07-2026.md](14-07-2026.md) — ✅ **RESOLVED** — terminal resize corrupts the screen (banner reset + mangled footer/input bar). Shipped Option B: resize now reflows the transcript in place (fresh screen redraws the banner responsively); readline's stray resize `>` stripped. See the Resolution section.
- [21-07-2026.md](21-07-2026.md) — Blocklist-purge prompt returns every launch: live init re-wrote catalog rows for blocklisted models because blocklist filtering wasn't centralized before `saveProviderCatalog`
- [22-07-2026.md](22-07-2026.md) — Ghost input frame after narrowing with a transcript up: the reflowed bottom UI was scrubbed only when an overlay was open; broadened the buffer repaint to all transcript-path resizes and made it wrap (not truncate) over-wide lines. Adds the `screenCounts` scenario primitive
- [24-07-2026.md](24-07-2026.md) — Blocklist-purge prompt STILL returned every launch: a stale row the API no longer serves, whose delete was applied to the local replica and swallowed on push, then conflict-wiped back from the primary. `deleteModelRows` now deletes straight against the primary on a synced store
- [24-07-2026b.md](24-07-2026b.md) — PTY test harness on Windows: resize test skipped (nested ConPTY never delivers a `process.stdout` resize event to the grandchild); session `start` deadline widened as hardening (never reproduced in the act)
- [24-07-2026c.md](24-07-2026c.md) — Turso still not enabled outside the repo: corrects 18-07-2026c — the real cause was `tryInjectDoppler()` in `src/index.ts` calling unpinned `doppler secrets download`, cwd-scoped just like `doppler run`; pinned `--project freecode --config dev`
- [24-07-2026d.md](24-07-2026d.md) — Root-directory grep died with an opaque `Command failed` while a subfolder search succeeded: the 10s `execFile` timeout SIGTERMs `rg`, and a signalled child has `code === null`, falling past the exit-1/exit-2 ladder into a rethrow that `--no-messages` had stripped of all detail. Timeout and maxBuffer kills now return actionable messages
- [24-07-2026e.md](24-07-2026e.md) — `npm test` crashed ~50% of runs with "Projects have different 'maxWorkers'" despite a single-project config, but never standalone: vitest 4 re-reads `os.availableParallelism()` once per spec file while grouping, and this phone's kernel isolates CPU cores under load. `vitest.config.ts` now pins `maxWorkers` from a one-shot sample
- [25-07-2026.md](25-07-2026.md) — Three pre-existing grep defects surfaced while extending the tool: `path` naming a file spawned `rg` with a file as its cwd (`ENOTDIR`); a malformed regex exited 2 with empty stdout and was reported as `No matches found` (a confident wrong answer — `--no-messages` blanks I/O warnings, not usage errors); and a `:` in a filename mis-parsed the result row. Fixed with a stat-based dir/file probe plus `--with-filename`, an stdout/stderr split on exit 2, and `--null` field separation
- [25-07-2026b.md](25-07-2026b.md) — Turns ended mid-task at 10 tool steps and surfaced as yellow `(empty response from model)`: `maxSteps: 10` in `loop.ts` plus two hardcoded copies of the same cap. Removed all three; `maxSteps` must be set to an unbounded value rather than omitted, since the AI SDK defaults it to `1`
- [25-07-2026c.md](25-07-2026c.md) — A tool call the SDK rejected before it ran (bad arguments, unknown name) ended the whole turn: neither ever reaches `execute`, so neither produces a tool result, and the AI SDK stops stepping when results don't match calls. The one retry that existed restarted from the *original* history, risking a replay of tool calls already executed. Rejections now go back to the model as failed steps and the turn continues from what actually ran
- [25-07-2026d.md](25-07-2026d.md) — Prompt-based tool mode told the model it had `spawn_agent` and to use it for big investigations, but `runParsedToolsLoop` builds `createTools` without a `spawnAgent` runner, so the tool never existed there. `buildSystemPrompt` now takes a `spawnAgent` flag and `loop.ts` rebuilds the prompt without it before falling back
- [26-07-2026.md](26-07-2026.md) — Rate-limit retry spammed the endpoint: `retryMaxWaitSeconds` clamped the server's own `retry-after` (guaranteeing a re-429 five times over), and 8 concurrent calls on one key each backed off in isolation then woke together. `retry-after` is now honored in full, a shared per-provider gate coordinates concurrent callers, and Anthropic — which had no fetch-layer 429 handling at all — now routes through `fetchWithRetry` too
- [27-07-2026.md](27-07-2026.md) — `tty-status-line-ticks` failed on Windows only: the fixture bought its mid-turn window with `sleep 4`, but `exec` runs through cmd.exe, which has no `sleep` — the tool errored out instantly and the turn finished before the assertion. Now sleeps via `node -e`
- [27-07-2026b.md](27-07-2026b.md) — Bare `vitest` showed 6 Windows-only failures, both causes test-side: the grep colon fixture cannot exist on NTFS (`we:ird.ts` silently becomes an alternate data stream on a file named `we`), and the PTY tests — excluded from `npm test` since they were written — were still collected by a bare `vitest`, where the daemon's 20s readiness budget loses to full-parallel contention. Colon test skipped on win32; PTY files now self-skip unless `FREECODE_PTY=1`. Also: `npm test` broke out of its section loop silently, so an e2e section that crashed mid-run (~46s total against a ~58s e2e section) looked like unit tests had been dropped from the pipeline — `test.ts` now names the failing section and what never ran
- [27-07-2026c.md](27-07-2026c.md) — `/config`, `/model` and `/eval` wiped the transcript and its scrollback (`\x1b[3J`) while leaving the conversation live, so a session that still resent everything looked fresh. Only `/clear` ever cleared history. The dispatcher now replays the transcript from `Conversation.messages` after each; repaint-from-screen-buffer was rejected because the pickers' own `\x1b[J` cleanup drops the buffer first. Ships alongside tool calls/results persisting into history at all
- [28-07-2026.md](28-07-2026.md) — A turn that produced nothing (Esc at a tool approval, a provider error, a `beforeAgentCall` that threw) still left the user's message in history, and the common failure path also persisted its error report as an assistant message the model then saw itself having written. The dispatcher no longer appends the user message up front: `Conversation.commitTurn` commits the whole turn or none of it, and `AgentLoopResult.error` carries failures separately from `text`. Closes the "Still open" of [27-07-2026c.md](27-07-2026c.md)
- [28-07-2026b.md](28-07-2026b.md) — The startup banner painted correctly then jumped up 3 rows as the footer/input UI drew: `setupFooterUI` left the cursor at (1,1) because DECSTBM homes it (so later `console.log`s overwrote the banner), and `setupInputUI` scrolled a fixed 3 rows even on a screen with 8 empty rows below the content. Now the region change is bracketed with cursor save/restore and the input frame opens its rows from the live cursor — via `writeChrome`, since bare newlines would otherwise be captured as transcript and corrupt overlay restore and resize. `bottom-ui.ts` split out `cli/chrome/ansi.ts` to stay under the line limit
- [29-07-2026.md](29-07-2026.md) — The replay added by [27-07-2026c.md](27-07-2026c.md) looked nothing like a real transcript: raw markdown, no dividers, no tool results, no edit diffs, a mis-coloured `> ` echo. The renderer was never coupled to the loop — `renderTurn` has always been data-in — but rendering from `Conversation.messages` cannot be faithful, since history holds a tool's result string and not the `oldText`/`newText` a diff needs. Now `cli/render/transcript-record.ts` records what the renderer actually put on screen and the replay hands it back to `renderTurn`; the test asserts live paint and replay are byte-identical. Splits the pure formatters into `transcript-format.ts` to stay under the line limit
- [29-07-2026b.md](29-07-2026b.md) — Two blank lines between response text and a tool call on the `mock:` path where the docs (and the replay of the same content) say one: `fake.ts` appends the step's terminating newline to the screen, but `fake-loop.ts` notified the step state machine with the model's raw text, so `textEndsWithNewline` went false and the lead-in emitted `"\n\n"`. `FakeModelResult.writtenText` now carries the text as painted. Caught by the new `transcriptBlock` assertion, which had itself been authored from the buggy screen
- [29-07-2026c.md](29-07-2026c.md) — `> /config` was recorded and replayed inside the conversation body, under a header reading "still sent to the model" — `session-modes.ts` recorded every submission before the dispatcher decided whether it was a command. Gated on a new `isSlashCommand`, shared with the dispatcher's own fallthrough so an unknown `/confgi` (also never sent) is treated the same
- [29-07-2026d.md](29-07-2026d.md) — `[cleanup] failed to remove tmpStore: EPERM` after the seeded blocklist e2e scenarios: `createClient` prepares a `SELECT 1` probe statement and native `libsql` `Statement` has no finalize, so the db file handle survives `close()` until GC — deterministic in a tight loop, flaky in a full run. Seeding moved to its own process, where exit releases it unconditionally; `rmSync` retries added as separate insurance for the child-held dirs
- [29-07-2026e.md](29-07-2026e.md) — ✅ **RESOLVED** — the intermittent e2e TTY segfault, cured: node-pty 1.1.0 → 1.2.0-beta.14 plus the ConPTY *host* pinned back to 1.23 (35/35 `--only-tty` and 10/10 `npm test` clean; the old pin crashed 2/5). Corrects 29-07-2026d's "Still open": the crash is *mid-run* (dies after 4–17 of 39 scenarios), not at `process.exit()`, so ConPTY-teardown-at-exit was never the cause. It is a data race on node-pty's file-scope `ptyHandles` vector — erased from the exit-callback thread while the JS thread held `pty_baton*` pointers into it — fixed by a mutex in **beta.13** (not by beta.11's deferred `connect()`, which still crashed 3/10). One package ships two independently versioned components with one defect each, so the answer was a version *plus* a host pin, which no single version can express: the ConPTY host bump at beta.12 took resize delivery from 15ms to ~1-1.5s and sometimes never, so `scripts/install/pin-conpty.cjs` restores 1.23 from a `conpty-pinned` alias dep and `run-e2e.ts` warns if the pin is gone. Also fixes the PTY leak (`driver.kill()` skipped `proc.kill()` for already-exited children, leaking 39 of 39 conout workers and pseudoconsoles per run) and a latent harness hole (`waitQuiet` returns instantly when the stream was already quiet, so steps asserted a screen the app hadn't been told to redraw). Records the dead end it went through first — concurrency capping, synthetic repros, and "no node-pty version has both" — under "Do not redo"
- [30-07-2026.md](30-07-2026.md) — A model writing `## The plan` put *nothing* on screen: the renderer's heading branch returned its "drop this line" `null` with no substitute, so the words were lost, not just the styling — and only when rendering was active (TTY/`FORCE_COLOR`), i.e. exactly the interactive case. ATX headings now render bold with the `#` markers (and any closing run) stripped, inline markup inside them preserved. The e2e scenario that had pinned the drop a day earlier now pins the bold text
- [30-07-2026b.md](30-07-2026b.md) — A read-only session was told it had `create`/`edit`/`shell_exec`: `buildSystemPrompt` hardcoded the full tool list while `createTools(readOnly)` offered three tools, so the model planned edits and burned steps on "no such tool". Generalises [25-07-2026d.md](25-07-2026d.md), which fixed the same class for `spawn_agent` with a single boolean and left the `readOnly` path open — the prompt now takes the actual tool list from `offeredToolNames`, pinned to `createTools` by a parametrised test. Also: `FREECODE_TRANSCRIPT_STREAM=null` never silenced *model text*, because `writeTranscriptText` bypassed the stream
- [30-07-2026c.md](30-07-2026c.md) — `npm run rate-limit-probe` silently probed nothing ("wrote 0 reports", exit 0): `d62da0f` deleted `ProviderConfig.type` when the Anthropic adapter was dropped but missed this script's `p.type === 'openai-compat'` candidate filter, which is now always false. Fix: every provider is openai-compat by construction, so `paid` + `SPECS[p.id]` is the whole filter
- [04-08-2026.md](04-08-2026.md) — A sweep report claimed "429 responses: 0 total, of which 39 were terminal": the terminal count was every error verdict, whatever caused it, printed on the 429 line. All 39 were the per-unit 300s timeout on a run where every request returned 200 — so the section defeated the handled-vs-terminal distinction `docs/sweeps.md` says it exists to make. Cause is now read from the unit's last attempt status, with a `last:` column separating an exhausted limit from a body that stalled mid-stream. Also: thrown plain objects reported as `[object Object]`, losing three nemotron errors entirely
- [11-08-2026.md](11-08-2026.md) — `intent-drift` reported every map link in a `@role` as drift (2 hits grown to 4, all false): the sweep prompt never said these claims are copied onto a map page, and `docs/map/` mirrors `src/` at identical depth, so `[wrappers.md](wrappers.md)` names `wrappers.ts`. Fixed as a translation rule, not an exemption — a link at the *wrong* module still fires, verified by control
- [05-08-2026.md](05-08-2026.md) — Esc at a tool approval, in two rounds. First: it discarded every completed step in that turn (not just the pending call), because Esc threw `UserAbortError` and unwound the whole `streamText` attempt. Making it an ordinary denial fixed that and removed `UserAbortError` entirely — but a denial is a tool *result*, and the AI SDK steps again whenever every call in the step has one, so the model was called again (and auto-denied again, and again) with no key the user could press: "auto-deny every further call this turn" was never a bound on the turn. Esc now ends the turn — `ToolCallConfirmation.stopTurn` makes `withTurnStop` throw `TurnStoppedError` *after* the usual denial has rendered, and a rejected `execute` is precisely what stops `streamText` stepping. The unpaired call that leaves behind is re-paired with its denial by `pairStoppedToolCalls`, without which the turn (user message included) would commit nothing at all. `stopRequested` deleted rather than patched; the decorator layer split into `agent/tools/wrappers.ts` at the line limit
- [05-08-2026c.md](05-08-2026c.md) — A failing `dotnet build` reported nothing but `Error: Command failed: dotnet build`: `shell_exec`'s catch returned `error.message`, which `exec` builds from stderr only — *stdout* is left on `error.stdout` and was dropped, so for the stdout-reporting build tools an agent runs most (dotnet, tsc, msbuild) 100% of the diagnostics vanished while the result still looked preserved. `shell_exec` now returns the command's real bytes and nothing else, plus a status line only for what those bytes cannot say — `[exit N]`, timeout, maxBuffer kill — and `maxBuffer` is raised off the 1 MB default that destroyed the same output a second way. Same defect [24-07-2026d.md](24-07-2026d.md) fixed in `grep.ts` and never generalized
- [06-08-2026.md](06-08-2026.md) — Zen turns died mid-flight on `The \`reasoning_content\` in the thinking mode must be passed back to the API` (HTTP 400): DeepSeek rejects an assistant `tool_calls` message with no `reasoning_content`, and AI SDK 3.4 drops the field while parsing the response, so every continuation request re-sent one without it. Looked flaky because zen load-balances between a `router-` upstream that does not enforce the rule and a `uuid` (Console) one that does — `big-pickle` pins to Console and fails 8/8, which is the deterministic repro. The check is on *presence*, not content, so the `zen` quirk now adds `reasoning_content: ''` to those messages and keeps no captured state at all
- [09-08-2026.md](09-08-2026.md) — Every documented interface, type alias and enum reached its map page stripped of its doc comment: `renderSymbol`'s branch for those three kinds emits `decl.getText()`, which starts *below* the leading trivia a JSDoc comment is, on a belief written into `docComment`'s own header that they "keep their documentation for free". The one file that seemed to confirm it had a documented first export. Found while probing whether a module header attaches to the following export — it does, blank line or not, which is why `@role`/`@readwhen` must be tags only
- [09-08-2026b.md](09-08-2026b.md) — A map page's section ran to the next heading and nothing else, so the last section inside a generated block absorbed any prose written under the block's closing marker — putting a paragraph about `startOverlayEpoch` inside `banner.md`'s `## Role`, the one section `npm run map -- role '**'` pulls across all 115 pages. The markers were dropped from bodies, which made them invisible as terminators too. Sections now stop at the next generated marker as well as the next heading, and the generated head is re-laid under the H1 rather than left wherever it first landed
- [10-08-2026.md](10-08-2026.md) — `npm run map-drift` graded two of its five criteria against sections the model was never sent: `stripGeneratedBlocks` removes *every* generated block, but the prompt described the removal as "auto-synced export signatures" and still asked for verdicts on "a stated purpose the file no longer has" and stale "read when" guidance. Stale since `@role`/`@readwhen` moved into generated blocks — the strip regex never changed, so nothing broke loudly; the prompt's description of it went stale underneath. The two criteria it could no longer reach are now `npm run intent-drift`, a second sweep that reads the tags from the source file instead of from the page
- [10-08-2026b.md](10-08-2026b.md) — The map's page-shape contract labelled a page's H1 `GENERATED` when nothing generates it: `upsertIntentBlock` only finds the H1 to splice its block below, and traffic runs the other way — `buildStructureBlock` reads each H1 through `pageLabel()` to title the structure tree. So the line naming which source file a page describes was authored, documented as machine-maintained, and checked by nothing; a renamed source file would leave a stale path in the H1 and regenerate the tree from it. The label now reads `AUTHORED`, and `check-map.ts` requires an H1 to start with its own source path and carry a label
- [10-08-2026c.md](10-08-2026c.md) — `logError` silently dropped every error unless `-log` was passed, defeating the `catch {}` → `logError(...)` sweep that `dd5c413` had done precisely to stop swallowing them. That commit deliberately deleted the `if (!enabled) return;` guard and said so; `7f4b36c`, a large test-coverage commit three days later, re-added the line as an unmentioned "supporting logger code" change, and a later test (`is silent when not enabled`) codified the revert as the contract. Caught by `intent-drift` reading `@role`'s "errors always surface" against the code — the tests had been siding with the regression for two months
- [10-08-2026d.md](10-08-2026d.md) — Every write on a local-only install threw `SyncNotSupported("File")`: all five write paths ended in an unconditional `c.sync()`, but a plain local-file client (the default, with no Turso configured) has no remote to push to. The `.catch()` handed each throw to `logError`, which [10-08-2026c.md](10-08-2026c.md)'s gate then dropped — so writes succeeded and nothing looked wrong for two months. Surfaced the moment the gate came off, as two stack traces scrolling the `/config` editor out of the viewport and failing `tty-config-toggle` on a UI string. `syncReplica` now no-ops unless the client is really a replica; the pure replica helpers moved to `db-replica.ts` to stay under the line limit
- [11-08-2026b.md](11-08-2026b.md) — Every map page came back dirty after `npm test` on Windows, each line count exactly one too high: Git for Windows sets `core.autocrlf=true` in the *system* config and `.gitattributes` covered only three files, so `countLines`' header-strip regex met `*/\r\n\r\n` — `[^\S\n]*` ate the `\r`, the two `\n?` stopped one newline short, and the blank line under the header survived as an extra line in every file. `.gitattributes` now pins `* text=auto eol=lf` (overriding autocrlf on every clone, `*.cmd` declared CRLF), `countLines` normalises line endings, and the BOM guard in `repo-encoding.test.ts` gained a CRLF sibling. Includes the recovery recipe for the phantom-dirty tree that follows an LF conversion — `git status` says 528 modified, `git diff` says 3, and `git add --renormalize .` fixes the stat data. Corrected later the same day: `--renormalize` only normalises what enters the *index*, so 276 files kept their CRLF bytes on disk and failed the new guard while `git status` stayed clean throughout. `git ls-files --eol` is what sees them; the entry carries the delete-and-re-checkout recipe
- [11-08-2026c.md](11-08-2026c.md) — A fully green unit run printed nine stack traces and shredded the dot reporter into fragments, all of it from tests doing their job: `db.test.ts` proving an unopenable store still starts, `provider-registry.test.ts` proving a 500 falls back to cache. Every line carried the `[error]` tag, so all of it was `logError`, which wrote unconditionally by design — and those writes bypass vitest's capture (no `stderr | tests/…` headers anywhere), landing on the parent's real stderr while the reporter was drawing. `logError` now returns early on `FREECODE_SILENCE_ERRORS`, set in `vitest.config.ts`; read per call so `logger.test.ts` can unstub it and still assert the write path. Rejected a `setupFiles` stub of `process.stderr.write` — one file, but it mutes the failing test's own diagnostics too. Explains why this gate is not [10-08-2026c.md](10-08-2026c.md) returning: that one was on by default in production, this one only exists under vitest
- [11-08-2026d.md](11-08-2026d.md) — A stale `SILICONFLOW_API_KEY` shredded the input frame a second after launch: `logError` wrote straight to `process.stderr`, and mid-session the cursor is parked at the typing caret *inside* the frame, so the write painted over it and both dividers. The startup model prefetch is only the reliable trigger — the same hazard covered ~30 call sites including every fire-and-forget DB persist. Logging now emits through `registerLogSink`, and `src/index.ts` registers `cli/tui-log-sink.ts` under the existing TTY guard (park in the scroll region → stdout, so the line survives a resize repaint → repaint chrome), alongside the retry-banner and quota sinks it copies. Second defect in the same line: the registry's cache fallback is fully handled and rethrows our own `new Error("HTTP 401")`, so the dumped stack named freecode's fetch wrapper and nothing else — new `logWarn` (always-on, one line, no stack) covers that level. Verified pre/post against the real TUI over PTY
- [11-08-2026e.md](11-08-2026e.md) — `undo-snapshot-on-first-write` could only ever pass on Linux: its scripted `shell_exec` read the shadow repo with `git --git-dir=$(echo "$FREECODE_HOME"/snapshots/*.git)`, and `shell_exec` runs through `exec`, which is cmd.exe on Windows — no command substitution, no `$VAR`, no globbing, so git got the text verbatim and the harness reported only a missing final line. The snapshot was on disk the whole time; the assertion never reached it. Now a `node -e` lookup, the convention the other fixtures already use, printing the directory listing when it finds no shadow repo so the next failure names itself
- [11-08-2026f.md](11-08-2026f.md) — Concurrent `takeSnapshot` calls died on `unable to write file …/objects/…: Permission denied` / `failed to insert into database`. The scratch index removed the `index.lock` contention, but the object database is still shared and is not lock-free on Windows: racers hash identical content, and Windows cannot rename over the existing read-only loose object where POSIX overwrites and git calls it a success. The hook swallows snapshot failures, so a session was left silently unprotected — likeliest on its first write, the only time snapshots fire, because a cold shadow repo writes the whole tree at once. `retryingObjectWrites` wraps `add`/`write-tree`/`commit-tree` (never `update-ref` or restore); an in-process lock was rejected because the real racers are two processes. 2/20 pre-fix, 0/120 post-fix with the retry observably firing
- [11-08-2026g.md](11-08-2026g.md) — `freecode -p --stats -m X "prompt"` exited 0 with a confident answer to the prompt `--stats`: every scan in `src/index.ts` was `indexOf`-based, so three tokens were dropped silently at once — `-p` swallowed `--stats` as its value, `-m` was an unknown flag nobody checked for (leaving the default model), and the real prompt was a positional the parser had no concept of. New `src/cli/args.ts` holds the flag table and one walk that must account for every token, rejecting a value that is itself a flag, an unknown flag, and a stray argument by name. Gating on the flag table rather than a leading `-` keeps `-p "--stats is what?"` working; positional-prompt was rejected as ergonomics that would still have run the reported command silently. `docs/commands.md` now states that a flag's value is the next argument, and that quotes are the shell's
- [12-08-2026.md](12-08-2026.md) — `npm run map -- role src/agent/` (and `src/agent/loop.ts`) errored with "No map page matches" while `exports`/`sections`/`neighbors-of` took those exact spellings: the two selectors were written independently and never reconciled. `normalizeMapPath` strips `src/` and rewrites `.ts` to `.md`; `matchesGlob` stripped only `docs/map/`, so a wildcard-free source path fell through to the directory-prefix rule and became `src/agent/loop.ts/**`, matching nothing. Glob verbs now normalize the same two source spellings before the prefix rule, so a path pasted from `git diff --name-only` — the workflow CLAUDE.md prescribes — works on every verb
- [12-08-2026b.md](12-08-2026b.md) — `checkpoint revert` printed `Branch main rolled back to a1b2c3d4.` while leaving the user standing on the agent's branch, at the agent's commit: `restoreSnapshot` moved `refs/heads/<branch>` and never touched `HEAD`, which is a symbolic ref, so a delegated run that did the ordinary `git checkout -b` restored the recorded commit onto a branch nobody was on. Files came back, so the tree looked plausible. The head-restore was also nested inside `if (existsSync(savedIndex))`, a different question, leaving HEAD alone whenever the index copy was missing. New `restoreHead` writes the branch ref *and* `symbolic-ref HEAD`, handles a snapshot taken while detached (`update-ref --no-deref`), and warns rather than throwing when `gc --prune=now` has collected the recorded commit. The first fix made `headRestored` mean "HEAD is now correct", which is true on every revert and would have announced a rollback each time — it means "history was rolled back", so the writes are gated on a pre-read that also keeps a clean revert out of the reflog. Found during the `checkpoint`/`shell_exec` security audit, but it fires for an honest agent
- [12-08-2026c.md](12-08-2026c.md) — `create .GIT/hooks/pre-commit` wrote the real hook, which then executed on the next `git commit`: all three checks in `git-guard.ts` compared bytes (`/\.git(?![\w-])/` twice, `segment === '.git'` once) on filesystems that do not, so NTFS and APFS opened `.GIT/hooks/pre-commit` as `.git/hooks/pre-commit` while the guard refused only the lowercase spelling. Now `/i` plus `toLowerCase()`; the Linux false positive is accepted rather than made platform-conditional. Case was the cheapest bypass, not the only one — `git config core.hooksPath` is the same RCE without the string `.git` appearing anywhere, and no regex over an unbounded shell closes that
- [12-08-2026d.md](12-08-2026d.md) — A delegated `-p --edit` agent could approve its own work: `freecode` is on `$PATH` and `shell_exec` is a shell, so one `freecode checkpoint accept` released the review lock, took a fresh baseline that made `checkpoint diff` report "No changes", left the payload on disk, and permitted the next delegation — the reviewer being shown an empty diff is the worst possible direction for that to fail in. Demonstrated end to end. `shell_exec` now stamps `FREECODE_SANDBOXED=1` on every child and `runCheckpoint` refuses `accept`/`revert` under it, before resolving a snapshot root so a refusal cannot leave an agent-triggered baseline behind; `list`/`diff` stay open, because reading is not approving. The marker is inherited, so it is stripped in `vitest.config.ts` and the e2e harness — otherwise `npm test` run *through* `shell_exec` fails every accept/revert case for a reason nothing in the output names. `env -u` defeats it by construction; the real fix is no `freecode` binary inside the sandbox at all
- [12-08-2026e.md](12-08-2026e.md) — An unwritable snapshot store silently switched off the one-`-p --edit`-per-project lock: `claimReviewLock` returned `undefined` for *claimed* and used a readback to tell "held" from "store broken", so when the readback failed too the answer was "nobody holds it" and the run started with mutual exclusion off. The comment defending it ("a broken safety net may not become a broken freecode") is right for `ensureSnapshot` and backwards for a precondition. Now `claimed | held | unavailable`, only the first proceeding, with the lock path and errno travelling on the refusal — fail-closed without a diagnosis just trades a silent hole for a mystery lockout. A half-written lock file now wedges `-p --edit` until deleted by hand
- [12-08-2026f.md](12-08-2026f.md) — Gitignored files were outside snapshot coverage in *both* directions: `add -A` honours `.gitignore`, so a payload in `dist/` or `node_modules/.bin/` was invisible to `checkpoint diff` and survived `checkpoint revert` with no `.gitignore` edit needed, while an agent that deleted an ignored `.env` or local DB had destroyed it permanently. The revert note ("files ignored by .gitignore were left as they are") was true and unactionable — it never named a path. All three staging calls now come from one `stagingArgs()` builder with `-f`, which is the point: restore deletes by diffing *its* index against the snapshot tree, so a restore staging less than the snapshot captured would leave behind exactly what the diff showed. `node_modules` excluded at any depth by pathspec (prevention, not backup — C3 mounts it read-only), `FREECODE_SNAPSHOT_EXCLUDE` replaces the list, and revert enumerates the excluded paths that exist. Also deleted the shadow repo's `info/exclude`, which `-f` overrides and which was therefore doing nothing; `.git` stays out because git skips any directory of that name. Cost: cold 3.0s → 12.4s (file count, not bytes; once per project), warm 0.20s → 0.28s, store 1.8 → 12.1 MB — and the cold walk broke two TTY scenarios from inside a `waitFor` budget until the e2e harness pre-warmed the shared store
- [13-08-2026.md](13-08-2026.md) — `git config core.hooksPath` was RCE that survived `checkpoint revert` and never appeared in `checkpoint diff`: `.git` was outside snapshot coverage, so config, hooks, refs and the reflog were whatever the agent left, and `branch -D` plus `reflog expire && gc --prune=now` put commits beyond any recovery a worktree snapshot could offer. No guard matched, because none of it contains the string `.git`. It could not be a wider `add` — git skips any directory of that name during the walk regardless of `-f`, and `read-tree --prefix=.git/` is refused by `verify_path` — so `.git` is now captured as a **second work tree** rooted at itself, its commit recorded as the snapshot commit's parent so one ref protects both. That collapses three mechanisms into one: the `.git/index` byte-copy is gone, `restoreHead` stays only for snapshots with no `.git` to restore, and a `.git` the agent deleted outright is rebuilt rather than mourned in a warning. `checkpoint diff` gained a `.git` section limited to `config` and `hooks/`, always raw — the trade being that a deleted branch is recovered without being shown. A locked file inside `.git` fails in two shapes (a delete git cannot do only warns; a *replace* it cannot do aborts mid-write, leaving `fsck` broken), and since `read-tree -u --reset` is idempotent the fix is to keep the worktree restore and say to run the revert again. Cost +12s cold, +0.1s warm, once per project
- [13-08-2026b.md](13-08-2026b.md) — A `-p --edit` run whose snapshot failed released the review lock and reported nothing: `ensureSnapshot` swallows failures to a log by design, `-p` silences the transcript, and `sessionSnapshotId()` returned `undefined` for both "never wrote" and "wrote and nothing covers it" — so a run that changed the project while its snapshot store was broken marked the project *free* at the moment it was least reviewable. Now three states (`none | taken | failed`), one effect each, with the failure on stderr and the lock kept; the old JSDoc's worry that this would strand the project is answered by `checkpoint accept`, which works with nothing snapshotted. The second half was `outstanding()`, which picked its target by timestamp and therefore selected a concurrent interactive session's *post-damage* snapshot, reporting a clean diff and then a successful revert against unreviewed state — the lock now carries the id its holder took, and a lock marked `snapshotFailed` refuses the bare verb rather than guessing. E2e breaks the store with `GIT_OBJECT_DIRECTORY`, which fails every shadow-repo git call while leaving the lock file claimable
- [13-08-2026c.md](13-08-2026c.md) — A file another process held open survived `checkpoint revert`, and in the worst case did it silently: a file git must *delete* and cannot is a `warning:` on **exit 0**, so agent-created content stayed on disk while the revert reported success and `runGit` dropped stderr. The plan predicted only the noisy half (a *replace* it cannot do, exit 128) and was wrong about it twice — `read-tree` takes tree-ish args, not a pathspec, so "re-run with those paths excluded" does not exist; and it is unnecessary, because `read-tree -u --reset` is not all-or-nothing: measured against a `FILE_SHARE_READ` lock it restores every other path, including ones sorting after the locked one, then reports what it could not touch. New `snapshots/locked-files.ts` reads that stderr and returns the paths, tolerating only the measured unlink shape; a locked path is a **failed** revert — exit 1, lock kept, paths named — because those paths still hold the agent's content. Also settles the question `13-08-2026.md` left open, against borrowing the `.git` half's "run it again"
