# Session Logs

This directory holds optional records of substantial feature work. Create a session log only when the user confirms after a new feature or significant change.

Use dated filenames:

```text
YYYY-MM-DD-short-feature-slug.md
```

Each log should include what was built, key decisions, files changed, and how to verify the work.
