we need to clean up the scripts:

  test-all-models
    tsx scripts/diagnostics/test-all-models.ts - does this belong here?
  time
    tsx scripts/pipeline/time.ts - are we wasting effort recreating something when a more bespoke solution exists
  test:pty
    vitest run tests/harness/pty/driver.test.ts tests/harness/pty/session.test.ts - i dont like this as a modifier to test. also it never gets run cos its not part of npm test ci pipeline. lets reconsider this
  unit:watch
    vitest - modifier for script that has not base ("unit" only) form. also alias is longer than what its aliasing
  docs:refresh-models
    tsx scripts/docgen/refresh-models.ts - didn't notice this was added. is it needed, and should it live here?
  verify:scenarios
    tsx tests/harness/run-scenarios.ts --no-build - again modifier with no base. 
  pty:session
    cross-env MSYS_NO_PATHCONV=1 tsx tests/harness/pty/session.ts - again modifier with no base.